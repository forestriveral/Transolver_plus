# Dataset (large, not versioned)
dataset/aircraft_dataset.zip
dataset/aircraft_dataset/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Training / eval artifacts (all under train/: logs, checkpoints, json, predictions)
train/
*.pth
*.pt
*.log
log_*.json
output/
metrics/

# Reference benchmark weights (kept local, not versioned)
checkpoints/

# Environments
.venv/
.env

# IDE
.vscode/
.idea/

# demo run (committed as a showcase deliverable, weights included)
!demo/
!demo/**
# but not the raw captured stdout (redundant with train.log/test.log)
demo/run_*.out
